# TinyCode ~10M — smallest footprint (good for 4GB GPUs)
model:
  size: 10m
  output_dir: outputs/tinycode-10m
  tokenizer: gpt2

dataset:
  name: codeparrot/github-code
  trust_remote_code: true
  streaming: true
  languages:
    - Python
  split: train
  text_column: code
  max_samples: null

training:
  block_size: 512
  per_device_train_batch_size: 8
  gradient_accumulation_steps: 2  # effective batch = 16
  num_train_epochs: 1
  learning_rate: 3.0e-4
  weight_decay: 0.01
  logging_steps: 100
  save_steps: 5000
  max_steps: null
  fp16: true
  seed: 42
